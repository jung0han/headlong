# Use one observer identity with scoped knowledge

One Observer Identity watches all Registered Projects so it can recognize the
user's cross-project preferences and recurring work patterns. A separate
identity per project would provide stronger isolation but fragment that
learning. Every observation and memory must therefore carry a global or
project-specific Knowledge Scope so a local decision is never applied to an
unrelated project.
