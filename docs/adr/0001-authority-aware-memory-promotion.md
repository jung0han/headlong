# Promote only user-authorized knowledge to active memory

The Observer may automatically promote explicit user statements and accepted
decisions, preferences, and constraints to Active Memory. Model-inferred facts,
patterns, and open loops remain Memory Candidates because treating inference as
user intent would give the model authority it does not have. Every promoted
memory retains evidence, and later changes supersede rather than erase earlier
versions so the decision history remains auditable.
